/**
 * Constantes del sitio publico que no dependen del API.
 *
 * `NOMBRE_DEL_SITIO` es el mismo texto del `<title>` de `index.html`: ninguna
 * fuente canonica define una marca, y el nombre del autor llega por
 * `GET /api/v1/profile`, que puede no existir todavia (decision D-009-N).
 */
export const NOMBRE_DEL_SITIO = 'Blog personal';
